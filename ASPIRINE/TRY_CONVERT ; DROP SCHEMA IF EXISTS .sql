TRY_CONVERT ; DROP SCHEMA IF EXISTS .CASCADE ;
REVOKE INPUT SCHEMABINDING FOR SELECT
FROM .CASCADE ; '(fun.c$tko)' ouverture delete from select loop
    int
end loop
  update table
        set field=value
    where condition elsif raise_application_error(-20000, 'message'); then function name(
      param in param_type
    ) return return_type;
      
  from table
where condition raise_application_error for if condition then case 
  when condition then dbms_output.put_line('message');
  else
end;
case 
  when condition then
  else 
end;
end if; in  loop
  c$tko
end loop
where condition
when value then delete from DROP
  where condition 